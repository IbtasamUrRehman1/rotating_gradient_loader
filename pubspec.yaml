name: rotating_gradient_loader
description: "A Flutter package that provides a rotating gradient loader widget."
version: 0.0.1
homepage: https://github.com/IbtasamUrRehman1/rotating_gradient_loader.git

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

platforms:
  ios:
  android:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

keywords:
  - flutter
  - loader
  - animation
  - custom
